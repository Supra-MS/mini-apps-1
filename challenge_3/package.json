{
  "name": "challenge_3",
  "version": "1.0.0",
  "description": "React Multi-Step CheckOut",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon server.js",
    "watch": "babel client --out-dir public --ignore node_modules --presets @babel/preset-react --source-maps inline --watch & live-server --open=./public/index.html --port=8080"
  },
  "author": "Supra",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.10.4",
    "@babel/core": "^7.10.4",
    "@babel/node": "^7.10.4",
    "@babel/preset-env": "^7.10.4",
    "@babel/preset-react": "^7.10.4",
    "live-server": "^1.2.1",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
